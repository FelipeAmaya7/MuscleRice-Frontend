{
  "name": "websitemusclerice",
  "private": true,
  "scripts": {
    "dev": "live-server --port=5173 --open=.",
    "start": "live-server --port=5173 --open=."
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "musclerice-root": "file:.."
  }
}
